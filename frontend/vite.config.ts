import { defineConfig } from 'vite';

// https://vitejs.dev/config/
export default defineConfig({
  // Vite should automatically pick up postcss.config.js and tailwind.config.cjs
  // No explicit css.postcss configuration needed here unless overriding defaults.
});